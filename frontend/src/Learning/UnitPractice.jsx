import { Link } from 'react-router-dom';
import { Code2 } from 'lucide-react';
import useRemote from '../Utils/useRemote';
import DataState from '../Components/DataState';
export default function UnitPractice({unitId}){const remote=useRemote(`/practice/units/${unitId}`);return <section className="rounded-2xl border border-indigo-200 bg-indigo-50 p-5"><h3 className="font-bold text-indigo-900"><Code2 className="mr-2 inline" size={20}/>Put your skills to work</h3><DataState {...remote}/>{remote.data.map(task=><Link key={task.id} to={`/learn/practice/${task.id}`} className="mt-3 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-indigo-100 bg-white p-4"><span className="font-bold text-indigo-900">{task.title}<small className="mt-1 block font-normal text-slate-500">{task.language_name || (task.language==='web'?'HTML / CSS / JavaScript':'JavaScript')} · {task.required?'Required practical':'Practice challenge'}</small></span><span className="text-sm font-bold text-purple-700">{task.passed?'Passed ✓':'Open coding lab →'}</span></Link>)}{!remote.loading&&!remote.error&&!remote.data.length&&<p className="mt-2 text-sm text-slate-500">Your trainer can add coding challenges to this unit.</p>}</section>;}
